input {
  udp {
    host 	=> "0.0.0.0"
    port 	=> 9303
    tags 	=> [<%= @udp_tags %>]
    codec 	=> "<%= @udp_codec %>"
  }
}

output {
  elasticsearch {
    host => '<%= @elastic_url %>'
	protocol => '<%= @elastic_proto %>'
  }
}
